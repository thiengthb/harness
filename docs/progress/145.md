# 145 — Template + phiên headless: gác fail-đóng không có đường thoả

issue: https://github.com/thiengthb/harness/issues/145
owner: @thiengthb · branch: `fix/145-template-headless-khong-the-thoa` · worktree: cây chính · platforms trong scope: n/a

## Trước lô này — trả lời, 2026-08-10

1. **Tiền đề còn đúng không?** Có, và nó không phải suy luận — đo trực tiếp:
   `CLAUDE_CODE_ENTRYPOINT=sdk-cli node tooling/gates.mjs --stage stop` → **exit 2**.
2. **Đã xây rồi chưa?** Không. Nhưng memory
   [[repo-template-phien-headless-stop-luon-do]] (phiên khác, cùng ngày) đã ghi đúng va chạm
   này — tôi tìm lại nó thay vì dựng lại từ đầu.
3. **Số suy ra hay đoán?** Đo. Mọi con số dưới đây có lệnh kèm output.
4. **Đo ở cây nào?** Cây chính + một cây TIÊU THỤ giả trong `tmpdir()`.

## 2026-08-10 (session 1, Claude + thien)

- **ĐÃ LÀM**
  - `gates.mjs`: thu hẹp phạm vi gác fail-đóng bằng
    `repoRole() === 'template' && declaredCommands(config()).length === 0`.
  - Nhánh cảnh báo mới — **cho qua nhưng NÓI RA** là không có gì được kiểm.
  - `test-hooks.mjs`: bảng gate **3 ca → 6 ca**, chạy trên HAI cây (repo này + cây tiêu thụ
    giả có `harness-manifest.json` trong `tmpdir()`).
  - Mutation: **5 mutant, 0 sống sót**.
  - `harness.version` → `2.50.0` + changelog.

- **BẰNG CHỨNG**
  ```
  ① TEMPLATE + headless        → exit 0, in "REPO TEMPLATE … KHÔNG có gì được kiểm"
  ② TIÊU THỤ giả + headless    → exit 2, "KHÔNG có người ngồi xem"   ← gác KHÔNG bị yếu đi
  ③ TEMPLATE + có người        → exit 0, không đổi
  test-hooks 220/220 (sàn 205)
  ```

- **CHƯA VERIFY ĐƯỢC — và tôi nói ra thay vì im**
  - Phần *"agent nay kết thúc được một lượt headless trong template"* **chưa đo end-to-end**:
    `claude -p` chạm session limit (`resets 1am`), bộ đếm 30 ngày đi **12 → 14**. Đó là một
    `?`, không phải một đảm bảo.
  - Cách kiểm khi quota mở:
    `echo "Trả lời đúng hai chữ: OK." | claude -p --max-turns 6 --permission-mode auto`
    **không** kèm `HARNESS_ALLOW_SKIPPED_GATES`. Xong = vá đúng.

- **TIẾP THEO**
  - Quota mở ⇒ chạy lại #144 (đo `eval − eval --bare`) trên tập an toàn
    `{0001, 0005, 0006, 0007}` — assertion của cả bốn chỉ ĐỌC.
  - `0002` (nguy hiểm) và `0003` (agent phải ghi file) thuộc `C:/project/eval-sandbox`, nơi đã
    có `eval-sandbox-origin.git` làm đích force-push vô hại.

- **QUYẾT ĐỊNH đã chốt**
  - **THU HẸP, không CẮT.** Gác vẫn đúng và vẫn mạnh ở repo tiêu thụ — nơi `commands` rỗng
    thật sự là cấu hình sai. Chỉ ca *"không có đường thoả"* mới được miễn.
  - **Điều kiện `declaredCommands()===0`, không chỉ `repoRole()==='template'`.** Template khai
    được 1 lệnh nghĩa là khai được thêm; bỏ 2 gate còn lại là **thiếu sót**, không phải cấu
    trúc. Ca ⑥ khoá đúng chỗ đó, và mutant M2 chứng minh nó không phải trang trí.
  - **Cho qua nhưng NÓI RA.** Im lặng biến ca *"không thể thoả"* thành ca *"đã thoả"* — đúng
    phép gộp mà cả `gates.mjs` tồn tại để chống.
