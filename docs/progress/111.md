# 111 — ngân sách giả định trả theo mức dùng; với gói PHẲNG cảnh báo luôn bật

issue: https://github.com/thiengthb/harness/issues/111
owner: @thiengthb · branch: `fix/111-ngan-sach-goi-phang` · worktree: gốc repo · platforms: n/a

## Trạng thái: CHẶN ở một dòng trong vùng cấm

Cơ chế xong và đo được. Suite `204/205`, và ca đỏ duy nhất **đúng**:

```
FAIL  budget ↔ bên đọc   plan — mã nguồn ĐỌC mà config KHÔNG khai
```

`harness.config.json` ∈ `paths.harness`. Agent không ghi được, và **không đi vòng qua guard**.

### Dòng cần thêm — `harness.config.json` → `budget`, ngay dưới `"alertAtPercent": 80,`

```json
  "plan": "metered",
  "$comment_plan": "metered = trả theo mức dùng (API pay-as-you-go): run-rate USD so với monthlyUsdCap. flat = subscription phẳng (Pro/Max): chi tiêu BẰNG ĐỊNH NGHĨA đúng bằng trần nên so USD luôn ra `over` và cảnh báo không bao giờ tắt — cổ chai thật là RATE LIMIT, đo bằng budget-alarm.log. Đè theo từng người ở .claude/settings.local.json → env.HARNESS_BUDGET_PLAN (gói cước là thuộc tính của người trả tiền, không phải của project).",
```

Hai đường: `/harness-propose`, hoặc DRI mở rồi bảo agent làm. Thêm xong ⇒ `205/205`, mở PR được ngay.

## Đã làm

- `budgetStatus({ plan, rateLimitHits })` — ba mode mới `flat-ok` · `flat-limited` · `flat-unmeasured`.
  `rateLimitHits === null` ⇒ `?`, **không** phải 0.
- `budgetPlan(cfg, env)` — hai tầng: `HARNESS_BUDGET_PLAN` (theo NGƯỜI) thắng `budget.plan` (theo ĐỘI).
  Gói cước là thuộc tính của người trả tiền; một đội có thể có người Pro phẳng và người API.
- `harness-doctor` đếm `rate_limit` trong `budget-alarm.log` 30 ngày (**12 dòng** đang có), và
  **thôi in** *"harness không đọc được hoá đơn"* ở nhánh phẳng — ở đó con số do chính harness đo.
- `rituals.mjs` phân nhánh cho cả ba mode. Không rơi xuống `ok` cuối hàm.
- 13 ca test mới (7 mode + 6 nguồn khai `plan`).

**`metered` không đổi một chút nào** — hai ca khoá riêng điều đó.

## Phát hiện phụ: cái gác chặn tôi có một lỗ, và tôi lách nó bằng một dấu `?`

Regex cũ: `[)\w]\.budget` — đòi ký tự liền trước `.budget` là `)` hoặc chữ.

Tôi chuyển phép đọc vào helper ở `lib` và gõ `cfg?.budget?.plan`; ký tự trước là `?` ⇒ **không
khớp** ⇒ suite chuyển sang `205/205` xanh. Suýt đọc sự im lặng đó là *"đã sửa xong"*.

Đã vá thành `[)\w]\??\.budget`. Gác đỏ trở lại vì lý do đúng.

Đây là **lần thứ ba trong một phiên** một phép kiểm dựa trên quét chuỗi tự phản bội nó (hai lần
trước ở #112: check tự khớp với chú thích của chính mình). Ba lần thì không còn là trùng hợp —
đáng một mục `/harness-propose` riêng.
