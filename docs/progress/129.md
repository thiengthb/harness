# 129 — Nghi thức native-slot-review: câu hỏi CỘNG về ô mở rộng native còn trống

issue: https://github.com/thiengthb/harness/issues/129
owner: @thiengthb · branch: `feat/129-native-slot-review` · worktree: cây chính · platforms trong scope: n/a (lớp tooling, không có UI)

## Trước lô này — trả lời, 2026-08-09

1. **Tiền đề còn đúng không?** Có, và đã đo lại chứ không tin tài liệu: `native-surface`
   ngày hôm nay in `31 sự kiện · 9 đang cắm · 22 để trống`, và cả 22 nằm trong rổ `na`.
2. **Đã xây rồi chưa?** `rg NATIVE_SLOTS|emptySlots|nativeSurface` toàn repo → ba mảnh có
   sẵn (`native-surface.mjs` đo, `claude-code-drift` hỏi câu TRỪ, `harness-doctor` lọc 5 ô
   cho consumer). **Không mảnh nào hỏi câu CỘNG.** Không có helper trùng chức năng để tái dùng.
3. **Số suy ra hay đoán?** Suy ra. `31` từ binary qua `nativeHookEvents()`; `9` từ
   `Object.keys(settings.hooks)`; `22` là hiệu. `109/104/102 ms` là đo 3 lần, không ước lượng.
4. **Đo ở cây nào?** Cây chính (`git worktree list` = 1 dòng). Không có ca `sparsePaths`.

## 2026-08-09 (session 1, Claude + thien)

- **ĐÃ LÀM**
  - Merge #128 trước (`c7b004c`, v2.46.0) — CI 7/7, gate `preMerge` chạy lại trên cây hiện
    tại: toàn WARN vì template chưa khai `commands.*`, và nó nói thẳng *"BỎ QUA — đây không
    phải pass"*. Phần đo thật nằm ở CI.
  - `nativeSlotState()` **thuần** ở `tooling/rituals.mjs`: `chua-xet = events − wired − ledger`.
    Là **phần bù**, nên sự kiện MỚI vendor thêm tự vào hàng chờ — không có danh sách viết tay.
  - Nghi thức thứ 14 `native-slot-review`, đặt ngay sau `claude-code-drift` vì hai mục là
    một cặp (TRỪ / CỘNG).
  - CLI `--slot <Event> <trạng-thái> "<lý do>"` và `--slots`. Lý do BẮT BUỘC; `co-viec` còn
    đòi `#N`.
  - `native-surface.mjs`: ba rổ thay cho một. Ô chưa xét vào `unknown`, không vào `na`.
  - Nạp 7 ô mà repo **đã có sẵn câu trả lời**: 2 × `khong-co-viec` (ADR 0002) và
    5 × `co-viec` → mở #130 · #131 · #132.
  - Test: 13 ca hàm thuần + 4 ca tầng nghi thức + 1 neo cấu trúc. Suite **217/217** (sàn 205).
  - Mutation: **8 mutant, 0 sống sót** (script ở scratchpad, bảng trong changelog §Mutation).
  - `harness.version` → `2.47.0`; changelog + `whats-new.md`.

- **TIẾP THEO**
  - `gh pr create` rồi đợi CI 7 job. Sau merge: `node tooling/rituals.mjs --slots` để thấy
    15 ô còn lại.
  - 15 ô `chua-xet` là việc thật, không phải nợ kỹ thuật của PR này. Xét dần, mỗi ô một
    lý do. Ô nào cũng có thể đóng bằng `khong-co-viec` — bác là một câu trả lời hợp lệ.

- **ĐANG VƯỚNG**
  - Không. Một chỗ suýt thành vướng: bản đầu để nhánh `wired === null ? null : …` trong
    `collect()` — không thuần ⇒ mutant M2 sẽ sống sót im lặng. Bắt được nhờ đúng bài học
    #127, trước khi chạy mutation. Đẩy vào hàm thuần thì nó thành một dòng trong bảng ca.

- **QUYẾT ĐỊNH đã chốt**
  - **Sổ đặt trong `.claude/claude-code-baseline.json`, KHÔNG theo đề xuất của chính mình**
    trong `.claude/learnings/2026-W32-cau-hoi-cong-va-may-do-config.md` (`.claude/state/…`).
    Lý do: `.claude/state/` **gitignore**. Phán đoán kiểu *"WorktreeCreate là provisioner"*
    là sự thật của đội — phải review được trong PR, phải sống qua đổi máy. Đúng chế độ hỏng
    đã đo với fixlog. Cái giá: baseline có BA người ghi ⇒ cả ba đọc-sửa-ghi trên `prev`,
    bảng `mergeBaseline` khoá cả ba khoá.
  - **Ô `co-viec` làm nghi thức XANH**, không giữ nó đỏ. Ranh giới của mục này là *"đã HỎI
    chưa"*, không phải *"đã LÀM chưa"* — bắt nó đỏ tới lúc ô được cắm là bắt nó đỏ vĩnh
    viễn (`lessons/0003` tầng 1). Đổi lại `co-viec` bắt buộc có `#N`, và số issue được in
    ở dòng `ok` mỗi lần `--all`.
  - **KHÔNG gộp với `NATIVE_SLOTS` của `harness-doctor`.** Bảng đó trả lời câu khác: *"5 ô
    template đã quyết là có việc — repo tiêu thụ đã cắm chưa?"*. Nó cố ý viết tay và cố ý
    không đổi theo binary. Phát hiện cũ của tôi (*"check không thể đỏ"*) **nói quá**: nó
    không đỏ ở **template**, và template là nơi cả 5 đã cắm — đúng hành vi, không phải bug.
