# 147 — Hết ngân sách ≠ agent làm sai

issue: https://github.com/thiengthb/harness/issues/147
owner: @thiengthb · branch: `fix/147-het-luot-khong-phai-lam-sai` · worktree: cây chính · platforms trong scope: n/a

## Trước lô này — trả lời, 2026-08-10

1. **Tiền đề còn đúng không?** Có, và nó đến từ một lượt đo thật (#144), không từ suy luận:
   `REGRESSION 0% (0/3)` với cả ba transcript là `Reached max turns`.
2. **Đã xây rồi chưa?** CÓ, cho một trạng thái khác: `infraFailure()` (#93) làm đúng việc này
   cho hạ tầng. Dùng lại khuôn đó — hàm thuần ở `lib`, bảng ca ở `test-hooks`, ca đầu-cuối
   bằng **agent GIẢ** ở `test-evals`. Không phát minh cách thứ hai.
3. **Số suy ra hay đoán?** Đo. Chữ ký `Error: Reached max turns (6)` là nguyên văn lần chạy
   2026-08-10, chép vào agent giả chứ không bịa.
4. **Đo ở cây nào?** Cây chính.

## 2026-08-10 (session 1, Claude + thien)

- **ĐÃ LÀM**
  - `budgetExhausted()` thuần ở `lib/harness.mjs` — **hàm riêng**, không thêm chữ ký vào
    `infraFailure`.
  - `evals/run.mjs`: `agent.budget` từ **HAI nguồn** (chữ trong output · `SIGTERM`),
    `measured` trừ thêm vế đó, và câu thứ **tư** trong nhánh `!measured`.
  - `fake-agent.mjs`: chế độ `maxturns` (in chữ ký ra **stdout**, exit **1** — đúng như thật;
    một agent giả exit 0 sẽ không tái tạo được bug).
  - Test: bảng thuần 8 ca + ca chéo · ㉑ ㉒ đầu-cuối · ④ đổi phép khẳng định.
  - Mutation: **5 mutant, 0 sống sót**.

- **BẰNG CHỨNG**
  ```
  test-hooks   221/221 (sàn 205)   budgetExhausted 8 ca, KHÔNG lẫn với infraFailure
  test-evals   25 ca OK            ㉑ trần LƯỢT · ㉒ trần WALL-CLOCK · ② vẫn FAIL đúng
  mutation     M1…M5 → 0 sống sót; M4 (nới regex) bị 6 ca chặn
  ```

- **CÒN LẠI — và tôi nói ra thay vì im**
  - Bản vá làm báo cáo **thành thật**, KHÔNG làm phép đo chạy được. Với `maxTurns` hiện tại,
    ba task kia thành `n/a` ⇒ mẫu số về 0 ⇒ vẫn chưa có `eval − eval --bare` (#144).
  - Việc còn lại là **hiệu chỉnh `maxTurns`**, và nó cần số đo: `0001` (10 lượt) đủ — ra 2043
    byte kết luận thật; `0005`/`0006`/`0007` thì không. Chưa làm vì chưa có số cho từng task,
    và đoán một con số ngân sách là đúng thứ bản vá này vừa dọn.
  - **KHÔNG chạy lại eval thật để xác nhận.** Ca ㉑ dùng chữ ký nguyên văn nên nó đã khoá đúng
    đường đó tất định; chạy thật chỉ thêm được một điều — chữ ký chưa đổi — mà tôi vừa đo sáng
    nay. Bộ đếm rate limit đang ở **14 lần/30 ngày**.

- **QUYẾT ĐỊNH đã chốt**
  - **Hàm RIÊNG, không thêm chữ ký vào `infraFailure`.** Khác biệt không ở cách nhận diện mà ở
    VIỆC PHẢI LÀM: hạ tầng thì *"chạy lại"* đúng; ngân sách thì chạy lại **cạn ở đúng chỗ đó**.
    Gộp hai cái là để một lời khuyên sai đi kèm một nửa số ca.
  - **HAI nguồn cho một trạng thái.** Trần lượt để lại chữ; trần wall-clock chỉ có `SIGTERM`.
    Bỏ nguồn thứ hai thì nửa lớp lỗi vẫn im — ca ㉒ khoá.
  - **Ca ④ đổi thứ nó khẳng định**, không xoá. ㉒ sở hữu phần "phân loại vào rổ nào"; thứ chỉ
    ④ khẳng định được là **phép cắt CÓ XẢY RA** — mất `timeout` thì runner vẫn in dòng hợp lệ,
    chỉ khác là in sau một phút. Nên ④ nay đo con số thời gian.
  - **Không nới phép nhận diện.** Ca ② (*agent exit 3 ⇒ vẫn FAIL*) là mốc chặn: exit khác 0 mà
    không có chữ ký ngân sách thì vẫn là thất bại thật.
