# 142 — Hai field cuối của máy dò config

issue: https://github.com/thiengthb/harness/issues/142
owner: @thiengthb · branch: `fix/142-hai-field-cuoi-may-do-config` · worktree: cây chính · platforms trong scope: n/a

## Trước lô này — trả lời, 2026-08-09

1. **Tiền đề còn đúng không?** Có, đo lại cả hai chiều: `mcp.maxTools` → `grep` toàn
   `tooling/ .claude/ .github/ evals/` ra **0 nơi đọc** (`maxServers` có 1:
   `harness-doctor.mjs:157`); `sessionPresenceMinutes` → `session-start.mjs:156` đang gọi
   `limit('sessionPresenceMinutes', 240)`.
2. **Đã xây rồi chưa?** Migration 003 đã CẮT một field theo cách này (`budget.modelTiering`,
   vá TEXT + dọn phẩy treo) — dùng lại đúng khuôn đó, không phát minh cách thứ hai.
3. **Số suy ra hay đoán?** Đo. `0 / 0` là output thật của `configCoverage()` chạy trên cấu
   hình MỚI. Migration chạy thật trên bản sao của config thật, không đọc bằng mắt.
4. **Đo ở cây nào?** Cây chính.

## 2026-08-09 (session 1, Claude + thien)

- **ĐÃ LÀM**
  - `harness-migrations/012-hai-chieu-cua-field-cau-hinh.mjs` + fixture
    `tooling/fixtures/migration-2.49.0/`. Hợp đồng `①②③④⑤` đạt, `$comment 4→5`.
  - Chạy thử khô trên **config thật**: JSON hợp lệ, máy dò về **0 không ai đọc · 0
    đọc-mà-chưa-khai**.
  - `harness.version` → `2.49.0` + changelog.

- **CÒN LẠI ĐÚNG MỘT BƯỚC — và nó bị khoá**

  `harness.config.json` nằm trong `paths.harness`. Đo trực tiếp: `process.env.HARNESS_DRI`
  là `null` trong tiến trình này. Theo luật của repo, **DRI bật cửa thoát, không phải agent**
  — và biến đi theo TIẾN TRÌNH nên phải khởi động lại, mang hội thoại theo:

  ```powershell
  $env:HARNESS_DRI='1'
  cd C:\project\harness
  claude --continue
  ```

  Việc đầu tiên sau khi vào: đo lại `process.env.HARNESS_DRI`. Vẫn `null` ⇒ biến chưa tới
  được tiến trình, **nói ngay, đừng mò**.

  Edit cần áp — đúng 3 chỗ, đã verify bằng chạy thử khô:

  ```diff
   # trong "limits", ngay sau "staleLockMinutes": 5,
  +    "$comment_sessionPresenceMinutes": "TTL của SỔ PHIÊN (.claude/state/sessions/). …",
  +    "sessionPresenceMinutes": 240,

   # trong "mcp"
     "mcp": {
  +    "$comment_da_cat_maxTools": "CẮT ở 2.49.0 — 0 nơi đọc …, VÀ tiền đề đã hết hạn …",
  -    "maxServers": 5,
  -    "maxTools": 20
  +    "maxServers": 5
     },
  ```

  Sau khi áp: `node tooling/harness-doctor.mjs` phải in `0 không ai đọc · 0 đọc-mà-chưa-khai`.

- **ĐANG VƯỚNG**
  - Chỉ vướng cửa thoát DRI. Không vướng kỹ thuật.

- **QUYẾT ĐỊNH đã chốt**
  - **Template và migration KHÔNG được lệch nhau.** Văn bản đích của config được **sinh ra
    bằng chính migration 012** chạy trên bản cũ, rồi mới áp vào template. Hai đường — thứ
    consumer nhận và thứ template có — không thể lệch vì chúng là **một phép biến đổi**.
  - **PHẢI có migration.** `harness.config.json` là SEED, bước copy của `upgrade` không chạm
    nó. Nửa THÊM là nửa quan trọng: máy dò 2.46.0 sẽ báo đúng phát hiện này ở MỌI repo con,
    và đó là phát hiện họ không tự hiểu được nếu template im lặng.
    Tiền lệ ngược: **2.35.0 cắt `budget.maxToolCallsPerRun` mà không kèm migration**, nên mọi
    consumer vẫn mang field chết đó tới hôm nay. Ngoài phạm vi PR này, nhưng đã ghi lại.
  - **Giá trị khai = 240, BẰNG ĐÚNG fallback đang chạy.** Khai số khác là dùng một migration
    mang danh "làm cho config đọc được" để lặng lẽ đổi hành vi.
  - **Cắt kèm bia mộ, vì tiền đề cũng hết hạn.** `$comment_mcp` viết *"Tool definition ăn
    context ở MỌI request"* — không còn đúng: MCP tool definition nay nạp **theo yêu cầu**.
    Cắt field mà để nguyên tiền đề hết hạn ngay cạnh là giữ lại một câu sai.
  - **Fixture dựng nhánh NGUY HIỂM NHẤT**, không dựng ca dễ: `maxTools` nằm cuối khối `mcp`
    (cắt ⇒ dấu phẩy treo), và một khoá do project tự thêm đứng ngay cạnh chỗ cắt.

## 2026-08-09 (session 2, Claude + thien) — bước bị khoá đã mở

- **ĐÃ LÀM**
  - Thiên bật `HARNESS_DRI=1`. **Đo trước khi làm gì**, đúng luật: `process.env.HARNESS_DRI`
    = `"1"` ở tiến trình con ⇒ hook thấy được.
  - Áp 2 Edit vào `harness.config.json` **qua tool `Edit`**, không qua Bash — cửa thoát chỉ
    có nghĩa khi nó đi đúng đường mà hook nhìn thấy.
  - Sổ audit `.claude/telemetry/harness-edits.log` ghi 2 dòng, `thi3n` + đúng nhánh + đúng
    file. Đó là thứ làm cửa thoát **review được sau**, khác hẳn việc tắt hook.

- **BẰNG CHỨNG**
  - `harness-doctor` → `✓ config: 49 field · 0 không ai đọc · 0 đọc-mà-chưa-khai · 1 cố ý
    vắng` — **dấu tick, không còn tam giác**.
  - **Hành vi**, không phải chỉ khai: `limit('sessionPresenceMinutes', 999)` trả **240**
    ⇒ giá trị đến từ config, fallback không còn được dùng.
  - **Nhất quán template ↔ migration**: chạy lại migration 012 lên config đã sửa ⇒ **no-op**
    (không ghi gì). Template khớp đúng đầu ra của migration, chứng minh chứ không khẳng định.
  - `test-migrations` 12/12 · `test-hooks` **217/217** (sàn 205).

- **TIẾP THEO**
  - Push + PR. Sau merge: repo con nhận thay đổi qua migration 012 khi chạy `upgrade`.

- **ĐANG VƯỚNG**
  - Không.
