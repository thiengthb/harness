# 134 — Xét 15 ô native còn lại, đóng câu hỏi CỘNG

issue: https://github.com/thiengthb/harness/issues/134
owner: @thiengthb · branch: `chore/134-xet-15-o-native` · worktree: cây chính · platforms trong scope: n/a

## Trước lô này — trả lời, 2026-08-09

1. **Tiền đề còn đúng không?** Có. `rituals.mjs --slots` sau khi merge #133 báo đúng 15 ô
   `chua-xet`.
2. **Đã xây rồi chưa?** Cơ chế xây ở #129. Lô này là **nội dung**, không phải cơ chế —
   không thêm dòng code nào.
3. **Số suy ra hay đoán?** **Đo.** Bảng `summary`+`description` rút thẳng từ binary
   (`CLAUDE_CODE_EXECPATH`, 274 MB, 30/31 sự kiện có mô tả). Đây là thứ Claude Code hiển thị
   trong `/hooks` — mạnh hơn tài liệu, cùng kỹ thuật ADR 0002 dùng để bắt
   `WorktreeCreate` là provisioner.
4. **Đo ở cây nào?** Cây chính.

## 2026-08-09 (session 1, Claude + thien)

- **ĐÃ LÀM** — xét đủ **15/15**, sổ nay **22/22 ô đã xét: 8 CÓ việc · 14 không**.

  **CÓ việc (3 ô mới → 3 issue mới)**

  | Ô | Việc, và vì sao nó là việc |
  |---|---|
  | `UserPromptExpansion` **#135** | `entropy-sweep` cắt skill bằng phép **ĐẾM** (12/12) vì nó không có dữ liệu skill nào từng được gọi. Phép đo duy nhất từng có là **gián tiếp** — suy từ artefact — và suy gián tiếp sai được: skill chạy rồi bỏ giữa chừng không để lại artefact nào |
  | `SubagentStart` **#136** | `AGENTS.md` đặt trần <5s ở `SubagentStop` vì *"nhân với tối đa 16 agent song song"* — **số 16 chưa ai đo**, và 2.1.224 bỏ trần 200 subagent/phiên nên hệ số nhân hết trần vendor che chở. Nhận xét này nằm trong baseline từ 2026-08-07 mà **chưa bao giờ vào hàng đợi** — đúng lớp lỗi #129 sinh ra để chặn |
  | `PermissionDenied` **#137** | harness đếm được guard **của chính nó** chặn bao nhiêu lần; không có số nào cho lần bị **bộ phân loại của vendor** chặn. Ca thật 2026-08-08: auto mode từ chối `Bash` nhiều lần liên tiếp, phải đi vòng suốt một đoạn dài, **0 dòng telemetry** |

  **KHÔNG có việc (12 ô mới)** — mỗi ô một lý do **và** một điều kiện mở lại:

  | Ô | Lý do bác (gọn) |
  |---|---|
  | `PostToolBatch` | `PostToolUse` đã đếm từng lời gọi; cái thêm là phép **nhóm**, chưa có câu hỏi nào cần. Thứ nó mời chào là chèn context — thuế ở mọi request |
  | `UserPromptSubmit` | điểm chèn context ở **mọi** request (LUẬT 2). Phần chặn đã có ở `PreToolUse`, và nó chặn ở chỗ **HÀNH ĐỘNG** chứ không chỗ **Ý ĐỊNH** — chính xác hơn hẳn |
  | `PostCompact` | harness **đã** nhận khoảnh khắc này: `SessionStart` cắm `matcher:"*"` nên nó bắn với `source=compact` — **đo được trong phiên này**. Và `PostCompact` chỉ in cho NGƯỜI, không chèn được vào model |
  | `PermissionRequest` | dựng **tầng cho phép thứ ba** cạnh `permissions.deny` và `PreToolUse`. `danger-zones.md` nói **thứ tự** hai tầng quan trọng hơn sức mạnh từng tầng |
  | `TeammateIdle` | sức mạnh của nó là *"prevent idle"* — **đẩy agent làm tiếp**. Harness đứng phía ngược lại: vấn đề trung tâm là *"agent tự khen, mark done sớm"* |
  | `TaskCreated` | đầu KIA của `TaskCompleted` (#131), và đầu kia đã có gác: DoR ở issue template. Task nhỏ hơn issue; bắt nó khai DoR là thủ tục thứ hai — và thủ tục thứ hai luôn là thủ tục bị bỏ |
  | `Elicitation` · `ElicitationResult` | **đo**: `mcp servers: 0`, `.mcp.json` không tồn tại. Mẫu số bằng 0 **do cấu trúc** — đúng nghĩa `n/a` |
  | `CwdChanged` | mọi công cụ neo qua `repoPath()` (suy từ vị trí **file**, không từ cwd) ⇒ đổi cwd không lệch phép đo nào |
  | `FileChanged` | chỗ duy nhất chưa phủ là **người** sửa config bằng editor — việc **hợp lệ**, không phải thứ cần gác. Config đọc lại ở mỗi lần gọi |
  | `DirectoryAdded` | `MULTI-PROJECT.md` giải bài toán nhiều repo bằng nhiều **phiên**, không bằng nhiều thư mục trong một phiên |
  | `MessageDisplay` | schema nói thẳng *"Display-only"*, và nó bắn theo từng mẩu văn bản — **đường nóng nhất trong cả 31 ô** |

- **TIẾP THEO**
  - Không đổi code ⇒ **không bump version**: `.claude/claude-code-baseline.json` bị
    `apply-to.mjs:207` cố ý loại khỏi bộ ship, nên hợp đồng nâng cấp không đổi.
  - #135 · #136 · #137 vào hàng đợi cùng #130 · #131 · #132 — **6 issue, 8 ô**.

- **ĐANG VƯỚNG**
  - **Vùng cấm.** `HARNESS_DRI` không đặt trong tiến trình này (đo trực tiếp). Nên 3 field
    config và mọi việc cắm hook (`.claude/settings.json` + `.claude/hooks/**`) tôi **không
    ghi được** — đúng thiết kế. Con đường hợp lệ là `/harness-propose`, và đó là skill
    **người gõ**.

- **QUYẾT ĐỊNH đã chốt**
  - **Bác là câu trả lời hợp lệ, nhưng phải kèm ĐIỀU KIỆN MỞ LẠI.** 12/12 ô bác đều có.
    Không có điều kiện mở lại thì `khong-co-viec` là một cánh cửa đóng vĩnh viễn, và sổ này
    biến thành thứ nó ra đời để thay: một câu trả lời không ai kiểm lại.
  - **Hai lý do tôi định dùng mà phải bỏ vì đo ra sai:**
    (a) định bác `TeammateIdle` bằng *"`teamSize = 1`"* — đo ra `project.teamSize` là
    **`null`** ở template (cố ý, nó là SEED). Lý do thay thế không dựa vào config nữa mà
    dựa vào **hướng** của ô: nó chống-dừng, harness cần dừng-và-kiểm.
    (b) định bác `PostCompact` bằng suy luận — thay bằng **quan sát trực tiếp**: phiên này
    in `SessionStart:compact hook success`, tức harness đã nhận đúng khoảnh khắc đó.

- **RỦI RO ĐÃ NHẬN DIỆN, chưa xử**
  - Repo tiêu thụ **khởi đầu với 22 ô `chua-xet`** (baseline không ship — cố ý, và lý do ở
    `apply-to.mjs:203-206` đúng). Nhưng 22 phán đoán là một lô việc lớn cho ngày đầu, và
    lô việc lớn ở ngày đầu là thứ bị đóng bằng lý do rập khuôn.
    **Cò đo được:** repo tiêu thụ đầu tiên chạm nghi thức này mà đóng cả 22 ô bằng lý do
    chung chung ⇒ cơ chế đã thành ô tích, phải đổi (ví dụ ship một SEED gồm những phán
    đoán ĐÚNG-Ở-MỌI-REPO: `MessageDisplay`, `PermissionRequest`, `TeammateIdle`).
    Chưa làm bây giờ vì chưa có repo nào chạm — làm trước là đoán.
